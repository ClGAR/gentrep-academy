export default function Loading() {
  return (
    <main className="auth-shell">
      <p className="eyebrow-dark">Staff</p>
      <h1 className="sec">Opening</h1>
      <p className="helper">Loading the check-in desk.</p>
    </main>
  );
}
