export default function Loading() {
  return (
    <main className="auth-shell">
      <p className="eyebrow-dark">Trainer</p>
      <h1 className="sec">Opening</h1>
      <p className="helper">Loading the verification queue.</p>
    </main>
  );
}
